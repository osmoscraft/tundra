{
  "name": "TinyKB Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {},
  "postCreateCommand": ". ${NVM_DIR}/nvm.sh && nvm install 16",
  "remoteUser": "node",
  "forwardPorts": [8080],
  "extensions": ["esbenp.prettier-vscode"]
}
