{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite build && vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^3.1.7",
    "vitest": "^0.24.1"
  }
}
